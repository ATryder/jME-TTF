buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        if( project.hasProperty('releaseUser') ) {
            classpath 'com.jfrog.bintray.gradle:gradle-bintray-plugin:1.+'
        }
    }
}

plugins {
	id 'java-library'
	id 'maven'
}

version='2.1.3'
group='com.atr'

sourceCompatibility = 1.8
compileJava {
    options.encoding = 'UTF-8'
    options.compilerArgs << "-Xlint:unchecked" << "-Xlint:deprecation"
}

repositories {
	mavenLocal()

    if( JavaVersion.current() == JavaVersion.VERSION_1_7 ) {
        // Fallback for JDK 7 that can no longer connect to jcenter with https
        maven { url "http://jcenter.bintray.com" }
    } else {
        jcenter()
    }
}

dependencies {
    api 'org.jmonkeyengine:jme3-core:[3.1,)'
    api 'org.jmonkeyengine:jme3-desktop:[3.1,)'
    api 'com.jaredrummler:sfntly:1.+'
    compileOnly group: 'com.google.android', name: 'android', version: '4.1.1.4'
}